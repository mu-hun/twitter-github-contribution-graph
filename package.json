{
  "name": "gh-graph",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "ts-node -- src/index.ts",
    "build": "ts-node -- src/index.ts",
    "lint": "./node_modules/.bin/eslint src/index.ts",
    "test": "run-p mock jest-alias",
    "mock": "serve mock",
    "jest-alias": "jest"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.15",
    "@types/node": "^13.1.6",
    "jest": "^24.9.0",
    "prettier": "1.18.2",
    "ts-jest": "^24.3.0",
    "ts-node": "^8.4.1",
    "typescript": "^3.7.4"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "serve": "^11.3.0"
  },
  "jest": {
    "roots": [
      "<rootDir>/src"
    ],
    "transform": {
      "^.+\\.ts?$": "ts-jest"
    }
  }
}
